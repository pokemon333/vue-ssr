{
    "version": 2,
    "routes": [
      { "handle": "filesystem" },
      { "src": "/test", "dest": "/test" },
      { "src": "/cars/:id", "dest": "/cars/:id" },
      { "src": "/(.*)", "dest": "/" }
    ]
  }
  